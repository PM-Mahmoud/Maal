import { createFileRoute } from "@tanstack/react-router";
import { useEffect,useState } from "react";
import { getMarketplace } from "@/lib/wealth-services.functions";
export const Route=createFileRoute("/_authenticated/app/marketplace")({component:Marketplace});
function Marketplace(){const [data,setData]=useState<any>();useEffect(()=>{void getMarketplace().then(setData)},[]);return <div className="mx-auto max-w-4xl p-6 space-y-5"><div><h1 className="text-2xl font-bold">Partner services</h1><p className="text-sm text-muted-foreground">Curated services with explicit, time-limited consent. Sponsorship never changes ranking.</p></div>{!data?.enabled?<div className="rounded-xl border border-amber-500/30 bg-amber-500/5 p-5"><h2 className="font-semibold">Marketplace not yet activated</h2><p className="mt-1 text-sm text-muted-foreground">The regulatory and commercial operating model must be approved before any partner product is shown.</p></div>:<div className="grid gap-4 md:grid-cols-2">{data.offerings?.map((o:any)=><article key={o.partnerKey} className="rounded-xl border border-border p-5"><h2 className="font-semibold">{o.name}</h2>{o.sponsored&&<p className="text-xs font-semibold text-amber-600">Sponsored relationship</p>}<p className="text-xs text-muted-foreground">Scopes: {o.scopes.join(", ")}</p></article>)}</div>}</div>}
