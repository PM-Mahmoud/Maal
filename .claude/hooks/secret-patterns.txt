# Shared secret patterns for .claude/hooks/pre-commit and scripts/ci-secret-scan.sh
# One extended-regex (ERE) pattern per line. Blank lines and lines starting with
# # are ignored. Keep in sync with the "Env vars" section of CLAUDE.md.

sk_live_[A-Za-z0-9]+
sk_test_[A-Za-z0-9]{20,}
re_[A-Za-z0-9_]{20,}
AZURE_OPENAI_API_KEY\s*=\s*[A-Za-z0-9]{20,}
BASIQ_API_KEY\s*=\s*[A-Za-z0-9]{20,}
ISAACUS_API_KEY\s*=\s*[A-Za-z0-9]{20,}
postgres(ql)?://[^:]+:[^@]+@
BEGIN [A-Z ]*PRIVATE KEY
